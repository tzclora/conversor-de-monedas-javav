package com.bubches.conversor;

public class ServicioCambio {

    public double convertir(String desde, String hacia, double cantidad) {
        // Esto luego se reemplazará con una llamada real a una API
        System.out.println("Simulando conversión desde " + desde + " a " + hacia);
        return cantidad * 1.1; // multiplicador simulado
    }
}
